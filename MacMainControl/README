1. 所有AppleScript存放在项目根目录/scripts下。
2. 按照AppleScript的类别进行分类，每类脚本一个目录。
3. 每个目录下models为数据项，每个目录为一张表，每个脚本在数据表里占据一行。
    直接读取目录获取脚本列表待研究。
4. views中定义页面种类，各方法决定页面输出。
5. urls中定义页面url，尖括号中可带参数。
6. 待解决：日志问题。AppleScript的输出最好可以print到网页上。